# ASR 依赖 (sherpa-onnx 版，低内存)
# 用 sherpa-onnx + SenseVoice int8 ONNX 替代 funasr+torch，
# 避免 1.8G 内存机器 OOM。
sherpa-onnx>=1.10.0
numpy>=1.24.0,<2.0.0
uvicorn>=0.20.0,<1.0.0
fastapi>=0.100.0,<1.0.0
python-multipart>=0.0.6,<1.0.0
