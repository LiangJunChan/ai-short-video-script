# 前端功能开关
# 注意：Vite 变量为构建期注入，修改后需重启 dev server 或重新 build 才生效。
# 敏感配置请改用 .env.local（已被 *.local 忽略），不要塞进本文件。

# 注册功能开关：true 开启（登录页显示注册入口、/register 可用），false 关闭
VITE_SIGN_UP=true

# 用户名注册开关：true 开启（注册页显示用户名注册 tab），false 关闭（仅邮箱注册）
VITE_USERNAME_SIGN_UP=false
